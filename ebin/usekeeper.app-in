{application, %APP_NAME%,
	[{description, "SigScale UseKeeper"},
		{vsn, "%APP_VERSION%"},
		{modules, [usekeeper_app,
				usekeeper_sup,
				usekeeper_server,
				usekeeper]},
		{registered, [usekeeper]},
		{applications, [mnesia, inets]},
		{env, [{export_dir, "export"},
				{hub_profile, hub},
				{hub_options, [{keep_alive_timeout, 4000}]}]},
		{mod, {usekeeper_app, []}}]}.
